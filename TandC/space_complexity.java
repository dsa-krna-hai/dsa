package TandC;

public class space_complexity {
    // likewise in time complex, here also we measure using asymptotic notations
    // 99% of the times, we do not consider input/output array

    // space complexity in recursive code
    // example: -> fibonacci
    // function f(n)
//    {
//        if(n==1 || n==0) return n;
//        return f(n-1) + f(n-2);
//    }
    // not all the functions are always available in the memory
    // at max we would be having n stacks in case of above example

    // example:
//    function f(n)
//    {
//        if (n<= 1) return 1;
//        return f(n-1) + f(n-2) + f(n-3);
//    }}
// time ->  O(3^n)
// space O(n)
}
