package Sorting;

public class partition_algo {
    //PURPOSE: al the elements smaller than the pivot are placed to its left,
    // & all the elements larger than the pivot are placed to its right
    // In place algo

    // if we have the access to the pivot
    // use the concept of 2 pointers
    // pivot = X
    // swap the pivot with the last element
    // apply partition algo in arr.length-1

    // thak gyii, easy it it will write after dinner <3
}
